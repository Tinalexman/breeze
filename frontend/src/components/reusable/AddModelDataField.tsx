import React, { FC } from "react";

const AddModelDataField: FC<{
  name: string;
  modelType: string;
  onChange: (target: string, val: string) => void;
}> = ({ name, modelType, onChange }) => {
  return <div></div>;
};

export default AddModelDataField;
